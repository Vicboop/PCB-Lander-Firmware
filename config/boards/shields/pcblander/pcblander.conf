# Ligar o modo de teclado dividido (Split)
CONFIG_ZMK_SPLIT=y

# Aumentar compatibilidade e estabilidade Bluetooth
CONFIG_BT_GATT_ENFORCE_SUBSCRIPTION=n
CONFIG_ZMK_BLE_EXPERIMENTAL_CONN=y
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y
